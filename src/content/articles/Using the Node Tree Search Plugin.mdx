---
author: Kai Magnus Müller
pubDatetime: 2023-03-10T14:22:00Z
title: Tree Navigator Feature Overview
draft: false
tags:
    - icons
ogImage: ''
description: Tree Navigator Plugin documentation and feature overview
---

import Figure from '@components/Figure.astro';

Tree Navigator is a plugin for Figma that makes it easier to navigate patterns with repeated elements like lists, tables or other complex designs, compared to box dragging or <kbd>CMD</kbd> + <kbd>Shift</kbd> - clicking individual layers.

Central feature of this plugin is the layer tree. When a layer is selected, the layer tree displays all of its ancestors up to the current page. By selecting a parent from the tree, the plugin selects all layers matching the selected element inside the chosen parent. This way, it is possible to quickliy select many repeating list elements, table cells and all other repeating layers.

<Figure
	alt="With the layer tree visualization, selecting similar layers in specific columns or screens becomes fast and easy."
	src="/video/tree-navigator/110423_intro_video.mp4"
	type="video"
	aspectRatio="16/10"
	fullWidth></Figure>

## Layer Tree

A common use case for the procedure described above is selecting all cells of one or more table columns. First, you select a cell to tell the plugin the layer name and type[^1] of the layers you want to find. The plugin then displays a tree view of all the parent elements of the selected layer up to the page level, for example the column frame, table frame, section frame and finally screen frame. Now you can define the selection scope, by selecting the parent in which the plugin should select all layers, for example the column frame. Finally, you have a selection of all cells in that table column.

<Figure
	alt=""
	src="https://res.cloudinary.com/dm3a0qioc/image/upload/v1680009276/LayerTree_02_i544hp.png"
	aspectRatio="16:10"></Figure>

### Scoping to sibling layers

Tree Navigator also works with multiple selected elements. As long as they share the same name and type, the layer tree will display the combined ancestor tree of all selected layers. Instead of being restricted to a parent -> child relationship for scoping the selection, this makes it really easy to get a selection only in specific screens or table columns for example. When multiple layers are selected, the layer tree will show an additional vertical selector that when clicked scopes the search to the topmost sibling layers of the selection.

<Figure
	alt="It is easy to target only specific frames for the selection."
	src="/video/tree-navigator/110423_sibling_select_demo.mp4"
	type="video"
	aspectRatio="16/10"></Figure>

## Layer Search

Tree Navigator also includes a free text search. Like the standard search in Figma, it is possible to enter a search term and to filter the results by layer type. Additionally, a search can also be scoped to the current selection or the current top level frame. The available filters are:

-   Layer type
-   Search scope
    -   Whole page
    -   Current selection
    -   Top level frame
-   Case sensitivity
-   Fuzzy and exact search

## Recent Searches

Performing a search will save the selected filter settings to make it easy to rerun common searches in the future. Recent searches are shared in a file, so you and your colleagues can access the same common searches.

<Figure
	alt=""
	src="https://res.cloudinary.com/dm3a0qioc/image/upload/v1681250483/search_scoping_kdbhwa.jpg"
	aspectRatio="16:10"></Figure>

[^1]: Internally, layers are called 'nodes' and they are separated into different types to give each of them custom properties and behavior. See also https://www.figma.com/plugin-docs/api/nodes for a list of all existing node types in Figma/FigJam.
