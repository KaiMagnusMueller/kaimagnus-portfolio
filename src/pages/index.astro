---
import { SITE } from '@config';

import { getCollection } from 'astro:content';
import Layout from '../layouts/Layout.astro';
import CollectionList from '@components/CollectionList.astro';
import InlineIcon from '../components/InlineIcon.astro';
import PageContentNav from '@components/PageContentNav.astro';

const articles = await getCollection('articles', ({ data }) => {
	return import.meta.env.PROD ? data.draft !== true : true;
});
const universityProjects = await getCollection('projects', ({ data }) => {
	return import.meta.env.PROD ? data.draft !== true : true;
});
const work = await getCollection('work', ({ data }) => {
	return import.meta.env.PROD ? data.draft !== true : true;
});

let allEntries = [...universityProjects, ...articles, ...work];

let featured = allEntries.filter((elem) => {
	return elem.data.featured.includes('noteworthy');
});

const headings = [
	{ text: 'Start', depth: 1, slug: 'start' },
	{ text: 'Highlights', depth: 2, slug: 'highlights' },
	{ text: 'Professional Work', depth: 2, slug: 'work' },
	{ text: 'Writing', depth: 2, slug: 'writing' },
	{ text: 'University Projects', depth: 2, slug: 'projects' },
];

// My technical background allows me to prototype, ship, and speak the same language as the developers in my team.
---

<Layout pageTitle={SITE.title} pageDescr={SITE.desc}>
	<Fragment slot="head">
		<link rel="preload" href="/kaimagnus-profile-2.jpg" as="image" />
		<link rel="preload" href="/200-20-5-monochrome.png" as="image" />
		<link rel="dns-prefetch" href="https://cdn.kaimagnus.de" />
		<link rel="dns-prefetch" href="https://res.cloudinary.com" />
	</Fragment>

	<PageContentNav {headings} />

	<main class="index-page color-text">
		<div class="hero--section color-text-low-contrast text text-large">
			<div class="hero--section-content-wrapper">
				<h1 class="color-text" id="start" data-toc-text="Start">
					Hi, I'm <InlineIcon
						src="/kaimagnus-profile-2.jpg"
						classList="avatar"
						alt="Kai Magnus Müller profile picture">Kai Magnus Müller</InlineIcon
					>,
				</h1>
				<p>
					a product designer and developer who specializes in complex systems and tools for experts. At Sopea
					I drive design, strategy and growth for a suite of HR products and use my coding skills to develop
					prototypes and collaborate with developers.
				</p>
				<p
					>I also teach Interaction Design at <a href="https://www.hfg-gmuend.de/">HfG Schwäbisch Gmünd</a> and
					led design for <a href="/work/epic-designing-internal-applications">insurance and investment</a>
					applications and <a href="/work/grid-component-system">design systems</a>
					at <a href="https://www.kaiser-x.com">Allianz</a>.</p
				>
			</div>
		</div>
		<section>
			<!-- Featured means noteworthy -->
			<CollectionList id="highlights" title="Highlights" entries={featured} />
		</section>

		<section>
			<!-- WORK -->
			<div class="insert-text">
				<p>
					Whenever I get that creative itch, I develop small hobby projects, like
					<a href="https://www.figma.com/community/plugin/1229905434435887252/">Tree Navigator</a> and <a
						href="https://www.figma.com/community/plugin/1289313190495880345">Icon Library Manager</a
					> for Figma, or <a href="https://collections-prototype.intersectionslab.com/">Collections</a>, an
					experimental platform for knowledge management to explore how computers can help us think and
					create.
				</p>
			</div>
			<CollectionList id="work" title="Professional Work" entries={work} />
		</section>
		<section>
			<!-- POSTS -->
			<CollectionList id="writing" title="Writing" entries={articles} />
		</section>
		<section>
			<!-- UNIVERSITY -->
			<div class="insert-text">
				<p>
					While studying, I wrote a bachelor thesis about <a href="projects/destinations"
						>building wayfinding systems</a
					>, designed a <a href="/projects/halo">browser built for research</a> (which inspired <a
						href="https://collections-prototype.intersectionslab.com/">Collections</a
					>), and went to Japan.
				</p>
				<p>
					I also prototyped an <a href="/projects/aralsea">interactive exhibit</a> during my time as a student,
					and we later developed an extended version for the exhibition
					<a
						href="https://www.uebersee-museum.de/ausstellungen/sonderausstellungen/vorschau/100-prozent-baumwolle/">
						100 Prozent Baumwolle</a
					> at Übersee-Museum Bremen.
				</p>
			</div>
			<!-- https://archive.is/3R3EH -->
			<CollectionList id="projects" title="University Projects" entries={universityProjects} />
		</section>
	</main>

	<style lang="css">
		.hero--section {
			display: flex;
			align-items: center;
			margin-block: clamp(4rem, 13vh, 8rem);
			max-width: 50ch;
		}

		section {
			margin-block-start: 8rem;
		}

		section:first-of-type {
			margin-block-start: 0;
		}

		.insert-text {
			max-width: 50ch;
		}

		section .insert-text p {
			margin-block-start: 0.5rem;
			margin-block-end: 0.5rem;
			color: var(--text-color-low-contrast);
		}

		h1 {
			margin-bottom: 0.5em;
		}

		@media (max-width: 768px) {
			.hero--section {
				margin-block: clamp(4rem, 10vh, 8rem);
			}

			section {
				margin-block-start: 6rem;
			}

			section:first-of-type {
				margin-block-start: 0rem;
			}
		}
	</style>
</Layout>
